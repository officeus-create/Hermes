# Claude Current Blockers and Owner Gates

Reviewed: 2026-07-31.

## PR #7 — restore frozen `#journey` anchor

Claude completed the additive anchor fix and local build/unit validation. The Cloudflare branch preview deployed successfully.

Remaining:

- verify the current branch against current `main` because the PR is stale;
- run or confirm the latest browser workflow tests;
- preserve the intentional decorative animated i-dot unless a separate design decision changes it;
- owner approval is required before merge.

This is not a production blocker for unrelated work. It can be rebased/recreated as a minimal current-main patch after conflict review.

## PR #13 — technical SEO sprint

Remaining:

- synchronize with current `main`;
- preserve recently merged Claude changes;
- reconcile shared `package.json` and `public/sitemap.xml` with PR #19;
- run the combined technical SEO, Careers, growth-registry, unit, build, and browser test suite;
- owner approval is required before merge/deploy.

## PR #19 — growth and Careers sprint

Completed safe work includes Careers, evidence registries, lane scoring, language/keyword registries, growth systems, route architecture, and the 200-task execution queue.

Remaining:

- synchronize the stale branch with current `main`;
- reconcile shared files with PR #13;
- preserve merged Claude PRs #15–#17;
- obtain green CI on the final synchronized head;
- owner approval is required before merge/deploy.

## Historical lane program

Blocked because the actual complete origin-to-destination route export has not been located in accessible sources. Do not infer lane direction from carrier addresses, remembered cities, or partial carrier databases.

Required input:

- origin city/state;
- destination city/state;
- service date or period;
- equipment type when available;
- source row/file identifier;
- privacy review.

## Academy pricing

The concepts `$999 for three months`, `$400 monthly`, and `from $600 monthly for corporate/employee training or consultation` must remain unpublished until the written program facts, payment/refund terms, country eligibility, and owner approval are recorded.

## Multilingual and location expansion

Blocked for publication until each route has dated evidence for:

- language/search demand;
- weak competition;
- real service capacity;
- unique local value;
- payment and sanctions/compliance eligibility where relevant;
- a response path in the advertised language;
- at least 7/10 publication score.

## Owner-only decisions

Claude, ChatGPT, Codex, and other agents must not perform these without a separate explicit instruction:

- merge to `main`;
- production deployment;
- DNS/Cloudflare account or billing changes;
- secret/API-key creation or exposure;
- destructive deletion or force-updating a protected branch;
- sending external communications;
- publishing unapproved pricing, compensation, employment, income, ranking, load, or direct-customer claims.
