name: Cloudflare deployment ownership contract

on:
  pull_request:
    paths:
      - wrangler.jsonc
      - wrangler.toml
      - wrangler.jsonc.example
      - wrangler.toml.example
      - config/cloudflare-pages-runtime-baseline.json
      - workers/lead-email/**
      - scripts/cloudflare-deployment-contract.test.mjs
      - scripts/cloudflare-production-hardening-contract.test.mjs
      - scripts/cloudflare-pages-runtime-baseline.test.mjs
      - scripts/check-connect-subdomain.mjs
      - scripts/connect-subdomain-verifier.mjs
      - scripts/connect-subdomain-verifier.test.mjs
      - .github/workflows/**
      - docs/CLOUDFLARE_DEPLOYMENT_OWNERSHIP.md
  push:
    branches:
      - main
    paths:
      - wrangler.jsonc
      - wrangler.toml
      - wrangler.jsonc.example
      - wrangler.toml.example
      - config/cloudflare-pages-runtime-baseline.json
      - workers/lead-email/**
      - scripts/cloudflare-deployment-contract.test.mjs
      - scripts/cloudflare-production-hardening-contract.test.mjs
      - scripts/cloudflare-pages-runtime-baseline.test.mjs
      - scripts/check-connect-subdomain.mjs
      - scripts/connect-subdomain-verifier.mjs
      - scripts/connect-subdomain-verifier.test.mjs
      - .github/workflows/**
      - docs/CLOUDFLARE_DEPLOYMENT_OWNERSHIP.md
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    name: Prevent duplicate repository-controlled Worker deployments
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify deployment ownership boundary
        run: node scripts/cloudflare-deployment-contract.test.mjs

      - name: Verify production hardening boundary
        run: node scripts/cloudflare-production-hardening-contract.test.mjs

      - name: Verify Pages runtime baseline
        run: node scripts/cloudflare-pages-runtime-baseline.test.mjs

      - name: Verify Connect production-readback quorum
        run: node scripts/connect-subdomain-verifier.test.mjs

      - name: Dry-run composed hermes-lead-email Worker
        run: npx --yes wrangler@4 deploy --dry-run --config workers/lead-email/wrangler.production.jsonc --outdir /tmp/hermes-lead-email-pr-dry-run
