name: Verify production domain on command

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write

concurrency:
  group: production-domain-command
  cancel-in-progress: true

jobs:
  verify:
    if: >-
      github.event.issue.number == 220 &&
      github.event.comment.body == '/verify-production-domain' &&
      contains(fromJSON('["OWNER","MEMBER","COLLABORATOR"]'), github.event.comment.author_association)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      - name: Check out approved main
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - name: Run strict public production verifier
        id: verify
        shell: bash
        run: |
          set +e
          node scripts/check-production-domain-issue.mjs
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Publish sanitized result to issue
        shell: bash
        run: |
          {
            echo "## Production custom-domain verification"
            echo
            if [ -f artifacts/production-custom-domain-check.md ]; then
              cat artifacts/production-custom-domain-check.md
            else
              echo "The verifier did not produce its sanitized report."
            fi
            echo
            echo "Verifier exit code: \`${{ steps.verify.outputs.status }}\`."
            echo "Checked approved \`main\` commit: \`${{ github.sha }}\`."
          } > /tmp/production-domain-comment.md
          gh issue comment 220 --repo "${{ github.repository }}" --body-file /tmp/production-domain-comment.md

      - name: Upload sanitized report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: production-domain-command-verification
          path: |
            artifacts/production-custom-domain-check.json
            artifacts/production-custom-domain-check.md
          if-no-files-found: error
          retention-days: 30

      - name: Enforce current live custom domain
        shell: bash
        run: test "${{ steps.verify.outputs.status }}" = "0"
