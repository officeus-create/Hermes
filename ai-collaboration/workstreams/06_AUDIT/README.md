# 06 AUDIT

Scope: independent QA, current-head verification, regression detection, evidence freshness, branch overlap, stale assumptions, release blockers and decision review.

Start with `../../00_READ_FIRST/LOCAL_AI_WORKSPACE.md`, then `../../../docs/AI_START_HERE.md`, `../../../docs/ERROR_REGISTER.md`, current PR/CI evidence, and the assigned audit mission.

AUDIT verifies and challenges. It should not become a parallel feature owner unless the task is explicitly reassigned after a handoff.
