# 03 HERMES CONNECT

Scope: Hermes Connect product and revenue builder — repair-shop onboarding, access and activation, booking, CRM, customer history, mobile/PWA behavior, localization and product growth.

Start with `../../00_READ_FIRST/LOCAL_AI_WORKSPACE.md`, then `../../../docs/AI_START_HERE.md`, the current Connect issue/mission, and the canonical Connect code under this same repository.

Do not create or revive a separate `hermes-connect-next` source repository. A legacy local path may exist only as an alias to `~/Hermes` after safe normalization.
